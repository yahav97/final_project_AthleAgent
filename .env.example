# AthleAgent — root-level env template (Docker / monorepo)
# For local Python development, prefer backend/.env (see backend/.env.example).

# FIREBASE_SERVICE_ACCOUNT_KEY=/app/backend/firebase-key.json
# LOG_DIR=/app/logs
# LOG_LEVEL=INFO
# ENABLE_LEGACY_SKLEARN_ENDPOINT=false
# ENABLE_TEST_PREDICT_ENDPOINT=false

# Full list of backend settings: backend/.env.example
