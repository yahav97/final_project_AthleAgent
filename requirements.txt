# Root alias for local install: pip install -r requirements.txt
# Canonical dependency list: backend/requirements.txt
# Docker / CI install from backend/requirements.txt directly — see docs/DOCKER.md
-r backend/requirements.txt
