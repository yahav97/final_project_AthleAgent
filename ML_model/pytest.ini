# ML policy gates + train-serve parity (also run in .github/workflows/backend-tests.yml)
[pytest]
testpaths = tests
pythonpath = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -ra
    --tb=short
